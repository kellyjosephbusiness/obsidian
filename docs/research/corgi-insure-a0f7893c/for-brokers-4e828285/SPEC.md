# SPEC — `/for-brokers` (source: https://www.corgi.insure/for-brokers, page-key `for-brokers-4e828285`)

Source full page: 1440×4281, 768 and 390 captures in `docs/design-references/corgi-insure-a0f7893c/for-brokers-4e828285/`.
Trees: `dom-1440.tree.txt`, `dom-768.tree.txt`, `dom-390.tree.txt` (grep the heading text to jump to a section). Copy: `page-text.txt`. Assets: `asset-manifest.json` (`local` paths are public URLs).

## FundLine framing
Broker / ISO partner page: "Small Business Lending, From Application to Funding." One broker workspace: applications, live offers, e-sign, payoff letters and funding confirmations. Header actions on this page: `Sign in` → `/sign-in`, secondary **"Get approved"** (become a partner) → `/for-brokers#apply`, primary **"Partner sign in"** → `/partners/sign-in` (source: Log in / Get appointed / Broker sign in). Footer CTA band on this page: "Grow your book with FundLine." / "Get approved and manage every client in one workflow." buttons: primary "Get approved" → `/for-brokers#apply`, white "Partner sign in" → `/partners/sign-in` (source: Grow your book with Corgi… / Get appointed / Broker sign in).

## Sections (main, 1440 top/height from `sections.json`)
1. **Hero** `section` [0,0,1440,480] — `pt-[158px] pb-16 px-16`, centered 940px column, gap 24: H1 60/60 -1.92px F37 Bolton 500 "Commercial Insurance, / From Quote to Certificate." → **"Small Business Lending, / From Application to Funding."**; sub `text-body-large` #4a4a4a max-w 720 → "Business lending in one partner workspace: applications, live offers, e-signature, payoff letters, and funding confirmations."; buttons row gap 16: primary small "Get approved", white small "Sign in". Decorative art: left hand-with-pen PNG (`hero-hand-figma.png`, 620px wide, `left:-88px top:calc(50%+48px) -translate-y-1/2 scale-x-[-1]`, hidden < md) and right stamp PNG (`hero-stamp-figma.png`, 430×403 box rotated -11.45°, right -72px). Both are in the manifest. Keep them (the stamp reads as "approved").
2. **Logo marquee** `section` [0,480,1440,81] — `py-6 border-b`, whole track at `opacity-50`, `logo-carousel-track` 60s `logo-scroll`, 4 copies, gap 80, grayscale logos (14 partner logos in manifest: artisan, athenahq, bland, deel, slash, eragon, intryc, origami, photon, judgment-labs, series, tandem, promptingco). Rebrand: render them as **text wordmarks** of fictional partner brokerages/lender partners (e.g. "Northgate Funding", "Blue Harbor Capital", "Meridian Business Loans", "Summit ISO", "Crestline Partners", "Harbor & Vane", "Ridgeway Lending", "Atlas Merchant", "Keystone Brokers", "Lumen Capital") in F37 Bolton 20px #191919 at the same 32px track height and 80px gaps; same 60s marquee, same 50% opacity. Do not ship real company logos.
3. **Workflow sentence** `section` [0,561,1440,540] — full-viewport-ish band, 1200px container with rails; one big paragraph (F37 Bolton? check tree: `font-mono` 28→40px, leading 1.3) where 5 phrases are inline **chips** (`inline rounded-[4px] px-1 py-1.5 bg-[#f0f0f0]`? read tree) each followed by a Material icon (`edit_document`, `request_quote`, `policy`, `payments`, `workspace_premium`). Behavior (from `behaviors.json`): each chip is a `button`/span with `aria-expanded`; clicking toggles a small expanded illustration/tooltip (innerText grows). Reproduce: chip click toggles `aria-expanded` and reveals a caption panel (opacity .6s) beneath the sentence; hover tint `.15s`. Copy → "Complete the application or send your client a link ✎, then get live offers 📄, accept terms and e-sign the agreement ✔, send a funding confirmation 💳, and issue payoff letters 🏅 all from one client record." (use the same five Material icons).
4. **Core products grid** [0,1101,1440,1246] — reuse `<PoliciesSection heading="Our Core Products, Ready to Offer" sub={undefined} cta={null} showMascots …>` — source heading "Our Core Coverages, Ready to Quote"; no sub, no bottom CTA; the specialty stack's link reads "See all financing types"; mascots present on the source (flying dogs at the section edges) — keep `showMascots`.
5. Footer via `<SiteFooter cta={BROKER_FOOTER_CTA} />`.

## Responsive
- 768: hero art hidden below md? (hand `hidden md:block`, stamp `hidden lg:flex` — verify in `dom-768.tree.txt`); sentence font 28px; products grid 2 columns.
- 390: H1 44px; sentence 24px; single column; logo strip heights via `--logo-mobile-*`.

## Files
- `src/app/for-brokers/page.tsx` (metadata title "For Brokers & ISO Partners | FundLine Capital").
- `src/components/sites/corgi-insure-a0f7893c/for-brokers-4e828285/{data.ts,BrokerHero.tsx,PartnerMarquee.tsx,WorkflowSentence.tsx}`; types `src/types/sites/corgi-insure-a0f7893c/for-brokers.ts`.
